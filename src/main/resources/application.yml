#Global configuration
spring:

  application:
    name: wotflix
    #H2 Configuration
    h2.console.enabled: true

  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/wotflix
    username: root
    password: pouledeau

  jpa:
    hibernate.ddl-auto: update
    show-sql: true

server:
  servlet:
    context-path: /wotflix

#Log level configuration
logging:
  level:
    error: DEBUG

