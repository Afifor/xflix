#Global configuration
spring:

  application:
    name: clanflix
    #H2 Configuration
    h2.console.enabled: true

  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/clanflix
    username: root
    password: pouledeau

  jpa:
    hibernate.ddl-auto: update
    show-sql: true

server:
  servlet:
    context-path: /clanflix

#Log level configuration
logging:
  level:
    error: DEBUG

